pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Githunt"

include(":app")
include(":common")
include(":data:api")
include(":ui:search")
include(":ui:ownerDetails")
