pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Githunt"

include(":app")
include(":data:api")
include(":ui:search")
include(":ui:ownerDetails")
